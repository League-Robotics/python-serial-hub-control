---
id: '001-014'
title: Flip pyproject packaging to src/rhsp and uv sync
status: open
use-cases:
  - SUC-009
depends-on:
  - '001-013'
---

# Ticket 014: Flip pyproject packaging to src/rhsp and uv sync

## Description

Update `pyproject.toml` to install `src/rhsp/` instead of `vendor/rhsp/`, run
`uv sync` to regenerate the lockfile, and verify that `import rhsp` resolves to
the new library. This is the gate-crossing ticket: after it, the repo installs
the new idiomatic library by default.

`vendor/rhsp/` is NOT deleted. It remains in the repository as a reference and
for on-hardware comparison with the new implementation.

## Acceptance Criteria

- [ ] `pyproject.toml` packaging source points to `src/` (or `src/rhsp/` specifically):
  - The `packages` / `package-dir` / `tool.hatch.build` or equivalent setting references `src/rhsp`
  - `vendor/rhsp` is no longer listed as the installed package location
  - `src/rhsp/protocol.json` is included in the package data
- [ ] `uv sync` runs without errors
- [ ] `uv lock` is regenerated and committed
- [ ] `python -c "import rhsp; print(rhsp.__file__)"` prints a path under `src/rhsp/`
- [ ] `python -c "import rhsp; print(rhsp.__file__)"` does NOT print a path under `vendor/rhsp/`
- [ ] `uv run pytest tests/` passes (no regression from the packaging change)
- [ ] `uv run pytest examples/` exits 0 (no hub needed; examples skip)
- [ ] `vendor/rhsp/` directory is still present and unmodified

## Implementation Plan

### Approach

The exact pyproject.toml change depends on the build backend in use. Common patterns:

**hatchling** (likely given `uv` usage):
```toml
[tool.hatch.build.targets.wheel]
packages = ["src/rhsp"]
```

**setuptools**:
```toml
[tool.setuptools.packages.find]
where = ["src"]
```

After editing `pyproject.toml`:
1. Run `uv sync` to update the virtual environment.
2. Verify `import rhsp` resolves to `src/rhsp/`.
3. Run the full test suite to confirm no breakage.

Include `protocol.json` in package data. For hatchling, this is automatic when
the file is inside the package directory. For setuptools, add:
```toml
[tool.setuptools.package-data]
rhsp = ["protocol.json"]
```

### Files to Modify

- `pyproject.toml` — packaging source change
- `uv.lock` — regenerated by `uv sync`

### Testing Plan

- `uv sync` exits 0.
- `python -c "import rhsp; print(rhsp.__file__)"` → path contains `src/rhsp`.
- `uv run pytest tests/` → all pass.
- `uv run pytest examples/` → all skip or pass.
- `uv run python -c "from rhsp.catalogue import COMMANDS; print(len(COMMANDS))"` → `72`.

### Documentation Updates

None required for this ticket (README was updated in ticket 013).
